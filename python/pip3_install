sudo apt install python3-pip
sudo apt-get install python3-matplotlib
pip3 install numpy==1.13.3
pip3 install --upgrade pip(pip3升级)（一旦升级后pip默认改为python3下面的pip）
pip2 -V 为python的pip
pip uninstall matplotlib (卸载)
